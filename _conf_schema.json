{
  "enable_plugin": {
    "description": "是否启用插件",
    "type": "bool",
    "default": true
  },
  "show_debug": {
    "description": "是否显示调试信息",
    "type": "bool",
    "default": true,
    "hint": "开启后会在日志和部分命令中显示详细的调试信息"
  },
  "max_nicknames": {
    "description": "每个用户最多缓存的昵称数量",
    "type": "int",
    "default": 3,
    "hint": "设置每个用户可以缓存的昵称数量上限"
  },
  "cache_expire_hours": {
    "description": "缓存有效期（小时）",
    "type": "int",
    "default": 168,
    "hint": "性别信息缓存的有效期，默认7天（168小时）"
  },
  "male_prompt": {
    "description": "男性用户的提示词",
    "type": "string",
    "default": "[用户性别: 男性]",
    "hint": "当检测到用户为男性时添加的提示词"
  },
  "female_prompt": {
    "description": "女性用户的提示词",
    "type": "string",
    "default": "[用户性别: 女性]",
    "hint": "当检测到用户为女性时添加的提示词"
  },
  "unknown_prompt": {
    "description": "性别未知用户的提示词",
    "type": "string",
    "default": "[用户性别: 未知]",
    "hint": "当无法确定用户性别时添加的提示词"
  },
  "prompt_position": {
    "description": "提示词插入位置",
    "type": "string",
    "default": "prefix",
    "options": ["prefix", "suffix"],
    "hint": "选择将性别提示词插入到用户消息的前面(prefix)还是后面(suffix)"
  }
}
